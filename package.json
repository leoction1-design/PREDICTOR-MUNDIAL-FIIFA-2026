{
  "name": "golcast",
  "version": "2.0.0",
  "private": true,
  "description": "GolCast — marcadores probables de futbol. La web vive en docs/ y la APK la envuelve.",
  "scripts": {
    "sync": "cap sync android",
    "apk": "cap sync android && cd android && ./gradlew assembleDebug",
    "doctor": "cap doctor"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/core": "^8.4.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.1"
  }
}
